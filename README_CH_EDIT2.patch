--- README_CH.md
+++ README_CH.md
@@ -282,6 +282,41 @@
 `# 查看圖表`
 `ls image/*.png`
 
+---
+
+## ⚡ 獨立 Python 腳本測試 (位於 `script/` 目錄)
+
+如果你只需要單一工具，或不想建置 ROS 2 工作空間，可以使用 `script/` 目錄中的強大獨立腳本：
+
+### 1. 純 DDS/ROS 2 Topic 頻寬與 FPS 監控 (`dds_network_logger.py`)
+此腳本可以獨立訂閱一個 topic，每秒統計接收到的資料量和 FPS，並在測試結束後**自動繪製圖表並輸出 CSV**:
+
+```bash
+cd /root/NETWORK/script
+python3 dds_network_logger.py \
+  --topic /camera/camera/color/image_raw/compressed \
+  --duration 30 \
+  --expected-fps 15.0
+```
+**特色**：提供接收到的實際 FPS 與給定預期 FPS (expected-fps) 之間的比較圖表，是針對串流資料的優良除錯工具。
+
+### 2. 純 DDS/ROS 2 Latency 與 Jitter 分析 (`dds_sub_image.py`)
+特別針對包含 timestamp 的高頻寬 ROS 2 訊息，可量測到訊息傳遞的 Jitter (抖動) 和 RTT，並繪製成統計圖。
+
+```bash
+cd /root/NETWORK/script
+python3 dds_sub_image.py
+```
+
+### 3. 無 ROS 依賴的純網路壓力測試 (`network_test_TCP.py`)
+直接使用 Python 執行 iperf3, ping 與 MTR 測試，適合前期開發：
+
+```bash
+sudo python3 script/network_test_TCP.py \
+  --target 192.168.0.230 \
+  --duration 15 \
+  --load B
+```
+
 ---
 
 ## 🎓 進階用法
